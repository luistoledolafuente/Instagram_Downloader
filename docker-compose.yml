version: "3.8"

services:
  app:
    build: .
    container_name: instagram-downloader
    ports:
      - "3000:3000"
    volumes:
      - ./downloads:/app/downloads
      - ./cookies.txt:/app/cookies.txt